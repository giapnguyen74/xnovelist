import { LevelModule } from '../types';

export const LEVEL_4: LevelModule = {
  level: 4,
  label: 'Drafter',
  blurb: 'AI sequences full draft proposals across scenes. (Coming in future versions)',
  actions: [],
};
