import { LevelModule } from '../types';

export const LEVEL_3: LevelModule = {
  level: 3,
  label: 'Co-writer',
  blurb: 'AI writes beats and scenes with you. (Coming in future versions)',
  actions: [],
};
